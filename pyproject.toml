[project]
name = "orchestrator"
version = "0.1.0"
description = "Service for orchestation of some services."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-on-whales>=0.77.0",
    "fastapi[standard]",
]
